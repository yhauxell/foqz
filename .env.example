# Used by `yarn dev:landing` / `yarn build:landing` for GitHub links (owner/repo).
VITE_LANDING_GITHUB_REPO=your-org/foqz

# Asset base path. Use `/` locally; for GitHub Project Pages use `/your-repo-name/`.
VITE_BASE=/
