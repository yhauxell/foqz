/** Default task card width (`getDefaultProps` / create tool). */
export const DEFAULT_TASK_CARD_W = 288
/** Default height for header + body (title, notes, blocked) + footer. */
export const DEFAULT_TASK_CARD_H = 300

export const MIN_TASK_W = 268
/** Minimum height so notes + footer are usable when resized. */
export const MIN_TASK_H = 260
